{
  "name": "afkmaxx",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "prisma generate && next dev -H localhost -p 4173",
    "build": "prisma generate && prisma migrate deploy && next build",
    "start": "next start -H localhost -p 4173",
    "check": "prisma validate && tsc --noEmit",
    "extension:pack": "node scripts/pack-extension.mjs",
    "desktop:pack": "node scripts/pack-desktop.mjs",
    "db:migrate": "prisma migrate dev",
    "db:push": "prisma db push",
    "postinstall": "prisma generate"
  },
  "dependencies": {
    "@clerk/nextjs": "^7.9.2",
    "@prisma/client": "^6.16.2",
    "@supabase/ssr": "^0.12.7",
    "@supabase/supabase-js": "^2.116.0",
    "next": "^16.3.5",
    "react": "^19.3.0",
    "react-dom": "^19.3.0"
  },
  "devDependencies": {
    "@types/node": "^26.5.1",
    "@types/react": "^19.3.0",
    "@types/react-dom": "^19.3.0",
    "prisma": "^6.16.2",
    "typescript": "^7.0.2"
  }
}
